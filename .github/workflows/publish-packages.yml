name: Publish Packages

on:
  release:
    types: [published]

permissions:
  contents: read

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      nuget-version: ${{ steps.version.outputs.nuget }}
      pypi-version: ${{ steps.version.outputs.pypi }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Resolve versions from spec and release tag
        id: version
        run: |
          BASE=$(jq -r '.info["x-space-gass-build"]' descriptions/preview/openapi.json)
          if [ -z "$BASE" ] || [ "$BASE" = "null" ]; then
            echo "::error::info.x-space-gass-build is missing from descriptions/preview/openapi.json"
            exit 1
          fi

          TAG="${{ github.event.release.tag_name }}"
          TAG_VERSION="${TAG#v}"

          if [[ "$TAG_VERSION" != "$BASE"* ]]; then
            echo "::error::Release tag '$TAG' does not start with spec version '$BASE'"
            exit 1
          fi

          SUFFIX="${TAG_VERSION#$BASE}"

          NUGET="${BASE}${SUFFIX}"

          if [ -z "$SUFFIX" ]; then
            PYPI="$BASE"
          else
            PYPI=$(echo "${BASE}${SUFFIX}" | sed -E \
              's/-alpha\.([0-9]+)/a\1/; s/-beta\.([0-9]+)/b\1/; s/-rc\.([0-9]+)/rc\1/')
          fi

          echo "nuget=$NUGET" >> "$GITHUB_OUTPUT"
          echo "pypi=$PYPI" >> "$GITHUB_OUTPUT"

          echo "### Versions" >> "$GITHUB_STEP_SUMMARY"
          echo "| Target | Version |" >> "$GITHUB_STEP_SUMMARY"
          echo "|--------|---------|" >> "$GITHUB_STEP_SUMMARY"
          echo "| Spec base | \`$BASE\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| Release tag | \`$TAG\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| NuGet | \`$NUGET\` |" >> "$GITHUB_STEP_SUMMARY"
          echo "| PyPI | \`$PYPI\` |" >> "$GITHUB_STEP_SUMMARY"

  publish-nuget:
    needs: prepare
    runs-on: ubuntu-latest
    environment: publish
    steps:
      - name: Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Setup .NET
        uses: actions/setup-dotnet@67a3573c9a986a3f9c594539f4ab511d57bb3ce9 # v4.3.1
        with:
          dotnet-version: "8.0.x"

      - name: Restore dependencies
        working-directory: sdks/csharp/client
        run: dotnet restore

      - name: Build
        working-directory: sdks/csharp/client
        run: dotnet build --configuration Release --no-restore -p:Version=${{ needs.prepare.outputs.nuget-version }}

      - name: Pack
        working-directory: sdks/csharp/client
        run: dotnet pack --configuration Release --no-build -p:PackageVersion=${{ needs.prepare.outputs.nuget-version }} -o ./nupkg

      - name: Push to NuGet
        working-directory: sdks/csharp/client
        run: dotnet nuget push ./nupkg/*.nupkg --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json --skip-duplicate

  publish-pypi:
    needs: prepare
    runs-on: ubuntu-latest
    environment: publish
    steps:
      - name: Checkout repository
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - name: Setup Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: "3.12"

      - name: Install build tools
        run: pip install build twine

      - name: Update version in pyproject.toml
        working-directory: sdks/python/client
        run: |
          sed -i "s/^version = .*/version = \"${{ needs.prepare.outputs.pypi-version }}\"/" pyproject.toml

      - name: Build wheel
        working-directory: sdks/python/client
        run: python -m build

      - name: Publish to PyPI
        working-directory: sdks/python/client
        run: twine upload dist/*
        env:
          TWINE_USERNAME: __token__
          TWINE_PASSWORD: ${{ secrets.PYPI_API_TOKEN }}

  # TODO: Re-enable test-examples job once NuGet/PyPI publishing is verified end-to-end.
  # test-examples:
  #   runs-on: ubuntu-latest
  #   needs: [publish-nuget, publish-pypi]
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4
  #
  #     - name: Extract version from spec
  #       id: version
  #       run: |
  #         VERSION=$(jq -r '.info["x-space-gass-build"]' descriptions/preview/openapi.json)
  #         if [ -z "$VERSION" ] || [ "$VERSION" = "null" ]; then
  #           echo "::error::info.x-space-gass-build is missing from descriptions/preview/openapi.json"
  #           exit 1
  #         fi
  #         echo "version=$VERSION" >> "$GITHUB_OUTPUT"
  #
  #     - name: Setup .NET
  #       uses: actions/setup-dotnet@v4
  #       with:
  #         dotnet-version: "8.0.x"
  #
  #     - name: Setup Python
  #       uses: actions/setup-python@v5
  #       with:
  #         python-version: "3.12"
  #
  #     - name: Wait for package index propagation
  #       run: sleep 60
  #
  #     - name: Update C# example package versions
  #       run: |
  #         find sdks/csharp/examples -name "*.csproj" -exec sed -i \
  #           "s|<PackageReference Include=\"SpaceGassApi\" Version=\"[^\"]*\"|<PackageReference Include=\"SpaceGassApi\" Version=\"${{ steps.version.outputs.version }}\"|g" {} +
  #
  #     - name: Build C# examples
  #       working-directory: sdks/csharp/examples
  #       run: dotnet build --configuration Release
  #
  #     - name: Update Python example requirements
  #       run: |
  #         find sdks/python/examples -name "requirements.txt" -exec sed -i \
  #           "s|space-gass-api==.*|space-gass-api==${{ steps.version.outputs.version }}|g" {} +
  #
  #     - name: Install and test Python examples
  #       run: |
  #         for req in $(find sdks/python/examples -name "requirements.txt"); do
  #           pip install -r "$req"
  #         done
  #
  #     - name: Commit version bumps
  #       uses: peter-evans/create-pull-request@v6
  #       with:
  #         token: ${{ secrets.GITHUB_TOKEN }}
  #         branch: examples/bump-${{ steps.version.outputs.version }}
  #         title: "chore: bump example package versions to ${{ steps.version.outputs.version }}"
  #         body: |
  #           Auto-bumped example package references to version **${{ steps.version.outputs.version }}** after successful publish.
  #         commit-message: "chore: bump example package versions to ${{ steps.version.outputs.version }}"
  #         delete-branch: true
  #
  #     - name: Open issue on failure
  #       if: failure()
  #       uses: actions/github-script@v7
  #       with:
  #         script: |
  #           await github.rest.issues.create({
  #             owner: context.repo.owner,
  #             repo: context.repo.repo,
  #             title: `Example tests failed for ${context.payload.release.tag_name}`,
  #             body: `The example tests failed after publishing version **${context.payload.release.tag_name}**.\n\nWorkflow run: ${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`,
  #             labels: ['bug', 'examples']
  #           });
