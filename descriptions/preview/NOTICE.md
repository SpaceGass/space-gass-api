# Preview Specifications

This folder contains **pre-release** OpenAPI specifications that are subject
to breaking changes without notice.

Place the preview spec here as `openapi-v15.0.0-preview.yaml` (or the
appropriate version number).

Once a version is formally released, its spec moves to
`descriptions/current/` and is archived in `descriptions/archive/`.
